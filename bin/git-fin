#!/bin/bash

set -ex

current_branch="$(git rev-parse --abbrev-ref HEAD)"

if [[ -n "$1" ]]; then
    branch="$1"
else
    branch="$current_branch"
fi

if [[ "$current_branch" == "$branch" ]]; then
    git checkout main
fi

git branch -D "$branch"
